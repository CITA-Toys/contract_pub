extends ../layout.pug
block title
    title faucet
block styles
    link(href='/styles/faucet.css' rel="stylesheet")
block content
    form.mainForm(action="/faucet/getNos" method="post")
        if success
            div.alert.success
                div.alertText Successful! hash: #{hash}
        else if alert
            div.alert
                div.alertText=alert
        div.mainTitle Appchain Festhet Faucet
        input#id-input-address.fontLittle.address(name='address', value=address, placeholder='Social network URL containing your E thereum address…')
        div.captcha
            input.fontLittle.captchaInput(name='captcha')
            span.captchaImg !{svg}
        button.fontNormal.submit(type='submit') GET
block scripts
    script(src='/scripts/faucet.js')